<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.iastate.room8">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Settings.UserSettings.UserSettingsActivity"></activity>
        <activity android:name=".Settings.RoomSettings.RoomSettingsActivity" />
        <activity android:name=".List.SubtaskActivity" />
        <activity android:name=".JoinRoom.NewUserRoomJoin" />
        <activity android:name=".Schedule.ScheduleDescriptionActivity" />
        <activity android:name=".Schedule.NewScheduleActivity" />
        <activity android:name=".Schedule.DayActivity" />
        <activity android:name=".Schedule.ScheduleMVP.ScheduleActivity" />
        <activity android:name=".List.ListActivity" />
        <activity android:name=".Bulletin.BulletinActivity" />
        <activity android:name=".List.MainListActivity" />
        <activity android:name=".List.NewListActivity" />
        <activity android:name=".RegisterLogin.RegisterActivity" />
        <activity android:name=".Home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterLogin.LoginActivity" />
    </application>

</manifest>